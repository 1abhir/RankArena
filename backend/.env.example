PORT=4000
JWT_SECRET=change_this_to_a_long_random_string
DB_PATH=./data/rankarena.db
# Optional: a GitHub personal access token used as a server-side fallback
# for unauthenticated rate limits (each user can also paste their own).
GITHUB_FALLBACK_TOKEN=
CORS_ORIGIN=http://localhost:5173
